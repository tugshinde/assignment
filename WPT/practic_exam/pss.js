let car = {
    brand: "BMW",
    model: "X5",
    year: 2023
};

let { brand, model, year } = car;

console.log(brand, model, year);let numbers = [10, 20, 30, 40];

let [first, second] = numbers;

console.log(first, second);